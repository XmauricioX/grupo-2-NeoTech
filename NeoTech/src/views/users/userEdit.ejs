<!-- HEAD -->
<%- include('../partials/head.ejs') %>

    <body class="body__register">

        <%- include('../partials/header.ejs') %>

        <%- include('../partials/nav.ejs') %>

            <!-- Registro -->
            <div class="">
                <h1>Editar Usuario</h1>
                <form class="" action="/cuenta/editar-usuario/<%= user.id %>?_method=PUT" method="POST" enctype="multipart/form-data">

                    <nav class="">
                        <!-- image -->
                        <p>Cargar foto de perfil</p>
                        <input type="file" name="user-image" multiple>
                        <!-- name -->
                        <label for="firstName"></label>
                        <input class="text" type="text" name="firstName" id="firstName" placeholder="Nombre" value="<%= locals.old ? old.firstName : user.firstName %>">
                        <% if (locals.errors && errors.firstName) { %>
                            <small class="text-error"><%= errors.firstName.msg %></small>
                        <% } %>
                        <!-- lastname -->
                        <label for="lastName"></label>
                        <input class="text" type="text" name="lastName" id="lastName" placeholder="Apellido" value="<%= locals.old ? old.lastName : user.lastName %>">
                        <% if (locals.errors && errors.lastName) { %>
                            <small class="text-error"><%= errors.lastName.msg %></small>
                        <% } %>
                            <!-- email -->
                        <label for="email"></label>
                        <input class="email" type="email" name="email" id="email" placeholder="Email" value="<%= user.email %>" readonly>
                        
                        <input class="text" type="text" name="tel" placeholder="Ingresá tu teléfono" value="<%= locals.old ? old.tel : user.tel %>">
                        <% if (locals.errors && errors.tel) { %>
                            <small style="color: red;"><%= errors.tel.msg %> </small>
                        <% } %>
                        <!--  -->
                        <input class="text" type="text" name="address" placeholder="Domicilio" value="<%= locals.old ? old.address : user.address %>">
                        <% if (locals.errors && errors.address) { %>
                            <small style="color: red;"><%= errors.address.msg %> </small>
                        <% } %>
                        <!--  -->
                        <input class="text" type="text" name="pc" placeholder="Código postal" value="<%= locals.old ? old.pc : user.pc %>">
                        <% if (locals.errors && errors.pc) { %>
                            <small style="color: red;"><%= errors.pc.msg %> </small>
                        <% } %>
                        <!--  -->
                        <select name="province" id="">
                            <option value="">Provincia</option>
                        </select> 
                        <!--  -->
                        <select name="city" id="">
                            <option value="">Localidad</option>
                        </select>
                        <!-- button edit -->
                        <button class="button-register" type="submit">Editar Cuenta</button>                    
                          
                    </nav>
                    
                </form>
            </div>


        <%- include('../partials/footer.ejs') %>

        <%- include('../partials/scripts.ejs') %>
    </body>

</html>